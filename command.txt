conda deactivate
uv --version
pip install uv
uv init "projectname"
uv pip list
uv pip init (create basic files)

create virtual env
===========
uv venv env
source env/bin/activate

uv pip install langchain
uv pip list

=================
uv pip install -r requirements.txt